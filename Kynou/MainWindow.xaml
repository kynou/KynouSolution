<Window x:Class="Kynou.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kynou"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Title="Kynou"
        Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" CornerRadius="5" GlassFrameThickness="0" ResizeBorderThickness="5"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <!-- Custom Title Bar -->
        <Border Background="{DynamicResource PrimaryHueDarkBrush}" Height="40" VerticalAlignment="Top"
                MouseDown="TitleBar_MouseDown">
            <Grid>
                <!-- Window Title -->
                <TextBlock x:Name="txtTitle" Text="Kynou | Welcome!" Foreground="White" FontSize="16" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="10,0,0,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <!-- Minimize Button -->
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Background="Transparent"
                            Foreground="White" ToolTip="Minimize" Click="MinimizeWindow_Click">
                        <materialDesign:PackIcon Kind="WindowMinimize" Width="24" Height="24"/>
                    </Button>

                    <!-- Maximize/Restore Button -->
                    <Button x:Name="btnMaxRestore" Style="{StaticResource MaterialDesignFlatButton}" Background="Transparent"
                            Foreground="White" ToolTip="Maximize/Restore" Click="MaximizeRestoreWindow_Click">
                        <materialDesign:PackIcon x:Name="iconMaxRestore" Kind="WindowMaximize" Width="24" Height="24"/>
                    </Button>

                    <!-- Close Button -->
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Background="Transparent"
                            Foreground="White" ToolTip="Close" Click="CloseWindow_Click">
                        <materialDesign:PackIcon Kind="WindowClose" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Margin="0,40,0,0">
            <TextBlock Text="Main Content Goes Here" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
